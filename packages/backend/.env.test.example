# Test environment configuration
# Copy this file to .env.test and adjust as needed
# Or set these variables in your shell before running tests

NODE_ENV=test
PORT=3021

# Test database configuration
# Uses a separate test database to avoid conflicts with development data
PGHOST=localhost
PGUSER=postgres
PGPASSWORD=password
PGDATABASE=dfac_test

# Or use DATABASE_URL directly:
# DATABASE_URL=postgresql://postgres:password@localhost:5432/dfac_test
# TEST_DATABASE_URL=postgresql://postgres:password@localhost:5432/dfac_test

# Test-specific settings
CORS_ORIGIN=*
LOG_LEVEL=error
